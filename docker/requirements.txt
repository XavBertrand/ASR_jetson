# --- Core utils ---
numpy
scikit-learn
tqdm
pydub
soundfile                  # nécessite libsndfile côté système
librosa==0.11.0
ffmpeg-python

# --- ASR (faster-whisper) ---
# NOTE: torch est installé par le Dockerfile selon l'arch (CUDA vs Jetson).
faster-whisper            # tire ctranslate2, tokenizers, sentencepiece

# --- Optionnel mais utile ---
huggingface_hub
onnx>=1.14
jiwer<4
marshmallow
pyloudnorm
resampy
wrapt
braceexpand
editdistance
wget
tensorboard

# ---- REMARQUE NEMO / TitaNet-S ----
# nemo_toolkit[asr] est lourd & sensible aux versions sur Jetson.

