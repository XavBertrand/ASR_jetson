# --- Core utils ---
numpy
scikit-learn
tqdm
pydub
soundfile                  # nécessite libsndfile côté système
librosa==0.11.0
ffmpeg-python

# --- ASR (faster-whisper) ---
# NOTE: torch/torchaudio sont installés par le Dockerfile selon l'arch (CUDA vs Jetson).
faster-whisper            # tire ctranslate2, tokenizers, sentencepiece

# --- Optionnel mais utile ---
huggingface_hub
onnx>=1.14
jiwer<4
marshmallow
pyloudnorm
resampy
wrapt
braceexpand
editdistance
wget
tensorboard

# ---- REMARQUE NEMO / TitaNet-S ----
# nemo_toolkit[asr] est lourd & sensible aux versions sur Jetson.
# Le Dockerfile expose WITH_NEMO=1 pour l’installer au build uniquement si tu le souhaites.
# nemo_toolkit[asr]==2.4.0
